/**
 * RTL Components - Tatx SA App
 * 
 * Standard RTL components based on the correct offer card pattern.
 * 
 * Usage:
 * ```javascript
 * import { 
 *   RTLContainer, 
 *   RTLCard, 
 *   RTLSectionHeader,
 *   RTLText,
 *   RTLHorizontalScroll,
 * } from '../components/RTL';
 * ```
 */

export {
  RTLText,
  RTLContainer,
  RTLCard,
  RTLSectionHeader,
  RTLHorizontalScroll,
  RTLRow,
  RTLColumn,
  RTLPresets,
} from './RTLComponents';
